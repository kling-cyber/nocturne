{
  "name": "nocturne-ai-murder-simulation",
  "version": "4.1.6",
  "private": true,
  "description": "NOCTURNE: server-authoritative multiplayer and single-player living mystery with local ComfyUI visual evidence generation.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "check": "node --check server/index.js && node --check server/game.js && node --check server/role-system.js && node --check public/app.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
